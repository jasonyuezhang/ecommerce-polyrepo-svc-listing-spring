# Development Dockerfile for Spring Boot Listing Service
# Note: Spring Boot DevTools provides hot reload capabilities
# Using non-alpine version for ARM64 (Apple Silicon) compatibility
FROM eclipse-temurin:17-jdk

WORKDIR /app

# Install Maven (Debian-based image)
RUN apt-get update && apt-get install -y maven && rm -rf /var/lib/apt/lists/*

# Copy pom.xml
COPY pom.xml .

# Download dependencies (cached layer)
RUN mvn dependency:go-offline -B

# Copy source code
COPY src src

# Expose ports (REST and gRPC)
EXPOSE 8080 9090

# JVM options for development
ENV JAVA_OPTS="-XX:+UseContainerSupport \
    -XX:MaxRAMPercentage=75.0 \
    -Dspring.profiles.active=docker \
    -Dspring.devtools.restart.enabled=true"

# Run with Spring Boot DevTools
CMD ["mvn", "spring-boot:run", "-Dspring-boot.run.jvmArguments=-Dspring.devtools.restart.enabled=true"]
